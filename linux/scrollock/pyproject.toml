[project]
name = "scrollock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Skellent", email = "skellent69@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "evdev>=1.9.3",
]

[project.scripts]
scrollock = "scrollock:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
